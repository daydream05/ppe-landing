{
  "private": false,
  "name": "hungrygatsbydev",
  "version": "1.0.0",
  "author": "Vince <ianparulan@gmail.com>",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "clean": "lerna clean",
    "deploy": "lerna run deploy",
    "empty-cache": "lerna run empty-cache",
    "init": "cd studio && sanity init && lerna run graphql-deploy",
    "format": "lerna run format",
    "graphql-deploy": "lerna run graphql-deploy",
    "lint": "lerna run lint",
    "now-deploy": "now && now alias",
    "postinstall": "lerna bootstrap",
    "sanity-deploy": "lerna run sanity-deploy",
    "start": "lerna run empty-cache && lerna run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-standard": "^14.1.1",
    "eslint-config-standard-react": "^9.2.0",
    "eslint-plugin-import": "^2.20.2",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-react": "^7.20.0",
    "eslint-plugin-standard": "^4.0.1",
    "lerna": "^3.20.2",
    "prettier-eslint-cli": "^5.0.0",
    "prop-types": "^15.7.2",
    "rimraf": "^2.6.3"
  },
  "dependencies": {
    "hungrygatsbydev-studio": "file:studio",
    "ppe-frontend": "file:web"
  }
}
